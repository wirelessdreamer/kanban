{
	"name": "@kanban/desktop",
	"version": "0.0.1",
	"private": true,
	"description": "Kanban desktop app — Electron shell for the Kanban runtime",
	"type": "module",
	"main": "dist/main.js",
	"scripts": {
		"clean": "shx rm -rf dist out",
		"build:ts": "tsc -p tsconfig.build.json && esbuild src/preload.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/preload.js",
		"build:mac": "npm run rebuild:pty && npm run build:ts && electron-builder --mac --config electron-builder.yml",
		"build:mac:dmg": "npm run rebuild:pty && npm run build:ts && electron-builder --mac dmg --config electron-builder.yml",
		"build:mac:arm64": "npm run rebuild:pty && npm run build:ts && electron-builder --mac dmg --arm64 --config electron-builder.yml",
		"build:mac:x64": "npm run rebuild:pty && npm run build:ts && electron-builder --mac dmg --x64 --config electron-builder.yml",
		"build:mac:universal": "npm run rebuild:pty && npm run build:ts && electron-builder --mac dmg --universal --config electron-builder.yml",
		"build": "npm run rebuild:pty && npm run build:ts && electron-builder --config electron-builder.yml",
		"dev": "tsc -p tsconfig.build.json && node scripts/launch-electron.mjs",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"rebuild:pty": "electron-rebuild -f -w node-pty",
		"postinstall": "electron-builder install-app-deps && node scripts/patch-node-pty.mjs"
	},
	"devDependencies": {
		"@electron/notarize": "^3.0.0",
		"@electron/rebuild": "^3.7.1",
		"@types/node": "^22.10.5",
		"electron": "^33.4.0",
		"electron-builder": "^25.1.8",
		"shx": "^0.4.0",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	},
	"dependencies": {
		"electron-updater": "^6.3.9",
		"kanban": "file:kanban-0.1.60.tgz",
		"node-pty": "^1.2.0-beta.11",
		"tree-kill": "^1.2.2"
	}
}
